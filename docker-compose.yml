services:
  cliptalk:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5180:5180"
    environment:
      HIGHLIGHT_HOST: 0.0.0.0
      HIGHLIGHT_PORT: 5180
      HIGHLIGHT_DATA_ROOT: /app/data
    volumes:
      - ./data:/app/data
    restart: unless-stopped

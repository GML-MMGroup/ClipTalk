# 普通本机使用无需创建 .env：启动后在页面“设置”中配置模型即可。
# 仅需改动端口或运行目录时取消下面注释。
# HIGHLIGHT_PORT=5180
# HIGHLIGHT_DATA_ROOT=./data
# HIGHLIGHT_MAX_WORKERS=1

# Docker 默认只允许本机访问。需要远程访问时，同时设置强访问令牌。
# CLIPTALK_BIND_ADDRESS=127.0.0.1
# CLIPTALK_INSTALL_PROFILE=cpu
# HIGHLIGHT_ACCESS_TOKEN=

# 不要在公共仓库提交自己的 API Key 或访问令牌。
# 服务器部署、GPU、TalkNet 等完整参数见 docs/environment.example。
# 安装与启动说明见 docs/getting-started.md。
